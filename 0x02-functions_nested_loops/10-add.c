#include "main.h"

/**
 * add - adds two integers and returns the result
 *@a, b: the targeted number
 *Return: result
 */

int add(int a, int b)
{
	int a, b, result;

	result = a + b;
	return (result);
}
