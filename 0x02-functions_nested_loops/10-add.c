#include "main.h"

/**
 * add - adds two integers and returns the result
 *@a: the targeted number
 *@b: the targeted number
 *Return: result
 */

int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
