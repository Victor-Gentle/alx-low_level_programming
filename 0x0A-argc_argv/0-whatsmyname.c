#include <stdio.h>

/**
 * main - prints its name, followed by a new line
 * @agrc: counts arguments
 * @agrv: array of arguments
 * Return: 0
 */

int main(int _attribute_((_unused_)) argc, char *argc[])
{
	printf("%s\n", argv[0]);
	return (0);
}
